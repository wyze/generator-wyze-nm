{
  "name": "<%= module %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "license": "MIT",
  "repository": "<%= github %>/<%= module %>",
  "author": {
    "name": "<%= name %>",
    "email": "<%= email %>"<% if ( url ) { %>,
    "url": "<%= url %>"<% } %>
  },
  "scripts": {
    "lint": "eslint src test",
    "pretest": "npm run lint",
    "test": "ava"
  },
  "main": "lib/index",
  "eslintConfig": {
    "extends": "wyze/base"
  }
}
